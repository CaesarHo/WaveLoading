<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="dialog_normal" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/fragment_left_in</item>
        <item name="@android:windowExitAnimation">@anim/fragment_left_out</item>
        <item name="android:backgroundDimEnabled">true</item>   <!--activity背景变暗 -->
        <item name="android:windowIsFloating">true</item>    <!-- 是否悬浮在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>   <!-- 半透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
